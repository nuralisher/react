import React, { ReactElement } from 'react'

interface Props {
    
}

export default function MessageItem({}: Props): ReactElement {
    return (
        <div>
            selected message
        </div>
    )
}
