import React, { ReactElement } from 'react'

interface Props {
    
}

export default function Messages({}: Props): ReactElement {
    return (
        <div>
            messages
        </div>
    )
}
